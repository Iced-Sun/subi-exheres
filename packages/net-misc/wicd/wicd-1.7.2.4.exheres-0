require wicd

PLATFORMS="~amd64 ~x86"

src_prepare()
{
    expatch -p0 ${FILES}/${PNV}-urwid-1.1.0-compatibility-1.patch
    expatch -p0 ${FILES}/${PNV}-urwid-1.1.0-compatibility-2.patch
    expatch -p0 ${FILES}/${PNV}-dbus-unicode-string.patch
    default
}

